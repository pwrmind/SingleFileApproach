<?php if (empty($app)): ?>
    <p>Приложение не найдено.</p>
<?php else: ?>
    <div class="card">
        <h1>Редактирование приложения</h1>
        
        <?php if (!empty($error)): ?>
            <div class="error"><?= htmlspecialchars($error, ENT_QUOTES) ?></div>
        <?php endif; ?>
        
        <?php if (!empty($success)): ?>
            <div class="success">Информация успешно обновлена!</div>
        <?php endif; ?>
        
        <form method="POST" action="?action=app_edit">
            <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf ?? '', ENT_QUOTES) ?>">
            <input type="hidden" name="app_id" value="<?= htmlspecialchars($app['id'] ?? '', ENT_QUOTES) ?>">
            
            <div class="form-group">
                <label for="title">Название приложения:</label>
                <input type="text" id="title" name="title" 
                       value="<?= htmlspecialchars($app['title'] ?? '', ENT_QUOTES) ?>" 
                       required minlength="3">
            </div>
            
            <div class="form-actions">
                <button type="submit">Сохранить изменения</button>
                <a href="?action=app_details&app_id=<?= htmlspecialchars($app['id'] ?? '', ENT_QUOTES) ?>">Отмена</a>
            </div>
        </form>
    </div>
    
    <div class="actions">
        <a href="?action=app_details&app_id=<?= htmlspecialchars($app['id'] ?? '', ENT_QUOTES) ?>">← Вернуться к деталям</a>
    </div>
<?php endif; ?>
