<h1>🚀 Публикация приложения</h1>

<?php if (!empty($error)): ?>
    <div class="error">⚠️ <?= htmlspecialchars($error, ENT_QUOTES) ?></div>
<?php endif; ?>

<div class="card">
    <form method="POST" action="?action=publish">
        <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf ?? '', ENT_QUOTES) ?>">
        <label>Название приложения</label>
        <input type="text" name="app_title" placeholder="Например: Фоторедактор Pro" required minlength="3">
        
        <label>Категория</label>
        <select name="category_id">
            <option value="">-- Выберите категорию --</option>
            <?php foreach (($categories ?? []) as $cat): ?>
                <option value="<?= htmlspecialchars($cat['id'] ?? '', ENT_QUOTES) ?>">
                    <?= htmlspecialchars($cat['name'] ?? '', ENT_QUOTES) ?>
                </option>
            <?php endforeach; ?>
        </select>
        
        <button type="submit" class="btn">Опубликовать</button>
    </form>
</div>