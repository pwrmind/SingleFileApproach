<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= htmlspecialchars($title ?? '', ENT_QUOTES) ?> | Dev Store</title>
    <style>
        body { font-family: sans-serif; padding: 20px; background: #f4f5f7; color: #222; }
        nav { background: #fff; padding: 12px; border-radius: 8px; box-shadow: 0 2px 4px rgba(0,0,0,.05); margin-bottom: 20px; }
        nav a { color: #007aff; text-decoration: none; font-weight: bold; }
        nav a:hover { text-decoration: underline; }
        .logout-btn { background:none; border:none; color:#ff3b30; cursor:pointer; font-weight:bold; font-size:16px; padding:0; margin-left:8px; }
        .logout-btn:hover { text-decoration: underline; }
        .card { background:#fff; padding:20px; border-radius:8px; box-shadow: 0 2px 4px rgba(0,0,0,.05); margin-bottom:15px; }
        input { width:100%; padding:8px; margin:5px 0 15px; box-sizing:border-box; border:1px solid #ccc; border-radius:4px; }
        .btn { background:#007aff; color:#fff; padding:10px 20px; border:none; border-radius:5px; cursor:pointer; font-weight:bold; }
        .btn:hover { background:#0066cc; }
        .error { color:#b00020; background:#ffe5e5; padding:10px; border-radius:5px; margin-bottom:15px; }
        .hint { color:#0060a8; background:#e5f1ff; padding:10px; border-radius:5px; margin-bottom:15px; font-size:14px; }
        main { max-width: 640px; margin: 0 auto; }
    </style>
</head>
<body>
    <nav>
        <?php if ($user ?? null): ?>
            <a href="?action=catalog">🏪 Каталог</a> |
            <a href="?action=publish">🚀 Публикация</a> |
            <a href="?action=profile">👤 Профиль</a> |
            <span><?= htmlspecialchars($user['name'] ?? '', ENT_QUOTES) ?></span> |
            <form method="POST" action="?action=logout" style="display:inline;">
                <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf ?? '', ENT_QUOTES) ?>">
                <button type="submit" class="logout-btn">Выход</button>
            </form>
        <?php else: ?>
            <a href="?action=catalog">🏪 Каталог</a> |
            <a href="?action=login">🔑 Вход для авторов</a>
        <?php endif; ?>
    </nav>
    <main>
        <?= $contentHtml ?? '' ?>
    </main>
</body>
</html>